<div class="row">
  <div class="medium-12 medium-centered columns">
    <%= form_tag previous_properties_path, method: 'post' do %>
      <% @broker_test.properties.each do |prop| %>
        <div class="medium-4 mediun-centered panel end columns">
          <div class="medium-5 columns">
            <%= image_tag(prop.image_url) %>
          </div>
          <div class="medium-7 medium-centered columns">
            <p><%= prop.address %> | <%= prop.sq_ft %> sq ft</p>
            <p><em><%= prop.description %></em></p>
            <%= check_box_tag "prop_id[]", prop.id %>
          </div>
        </div>
      <% end %>
      <%= hidden_field :lead_id, :value => @lead.id %>
      <%= submit_tag "Submit" , class: "button cust-btn"%>
    <% end %>
  </div>
</div>


    
  